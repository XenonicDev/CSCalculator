using System;

namespace CSCalculator
{
    public enum Symbols
    {
        Add = '+',
        Subtract = '-',
        Multiply = '*',
        Divide = '/',
        Negate = '~',
        Caret = '^',
        Exponential = 'E'
    }
}
